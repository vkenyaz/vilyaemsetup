# /home/vilyaem/.config/tmuxinator/work.yml

name: work
root: ~/
startup_window: sigma
startup_pane: 1
windows:
  - sigma:
      layout: main-horizontal
      panes:
        - cd ~/j/ ; j
        - vit
        - pomotask
  - workshop:
      layout: horizontal
