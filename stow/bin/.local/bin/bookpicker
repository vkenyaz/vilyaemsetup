#!/bin/sh
cd ~/b/ || exit 1

dir=$(\ls | dmenu -l 30 || exit 1)
book=$(\ls "$dir" | dmenu -l 30 || exit 1)

if [ -z "$book" ]; then
    echo "No file selected."
    exit 1
fi

software=$(printf "sioyek\nkoreader\n" | dmenu || exit 1 )

"$software" "$dir/$book"
